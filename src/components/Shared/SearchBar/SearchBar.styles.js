const { StyleSheet } = require('react-native');

const styles = StyleSheet.create({
  searchBarContainer: {
    flexDirection: 'row',
    alignItems: 'strech',
    gap: 10,
  },
  inputWrapper: {
    backgroundColor: '#fff',
    borderRadius: 10,
  },
});

export default styles;
